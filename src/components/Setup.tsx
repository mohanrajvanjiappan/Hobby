let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }xlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }2let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }klet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }klet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }klet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }klet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }2let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }2let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }vlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }?let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }?let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }?let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }?let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }?let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }xlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }5let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }vlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Vlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }vlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }vlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }0let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }6let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }4let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }5let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }<let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }]let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }!let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }!let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }klet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Qlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }zlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }|let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }'let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    };let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }(let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    })let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }>let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }=let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }[let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }2let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }3let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }4let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }5let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }6let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }7let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ylet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }8let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }9let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }jlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }0let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }}let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }{let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }1let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }2let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }blet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }dlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }Flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }flet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }nlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    },let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }
let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ilet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }mlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }elet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }_let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }ulet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    } let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }"let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }hlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }tlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }plet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }:let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }wlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }slet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }.let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }/let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }clet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }alet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }rlet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }-let assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }llet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }olet assumedType = firstType || quizType || 'multiple-choice';
    if (firstTitle.toLowerCase().includes('rapid') || firstTopic.toLowerCase().includes('rapid')) {
      assumedType = 'rapid-fire';
    }
    if (jsonItemsCloned.some(item => item.title && item.title.toLowerCase().includes('rapid'))) {
      assumedType = 'rapid-fire';
    }glet assumedType = fir